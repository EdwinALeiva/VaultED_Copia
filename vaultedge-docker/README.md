# VaultEdge Docker Environment

This folder contains the Docker setup for running PostgreSQL and pgAdmin locally for the VaultEdge backend application.

---

## 🚀 How to Start

To launch the environment:

```bash
docker compose up -d
